Check that the release number in the build.xml file is correct. Otherwise
update and commit.

This project is now handled with git flow and uses the branching strategy 
thus defined. The actual release is eventually built from master with:

ant build-dist

# Den färdiga ZIP-filen finns på toppnivån och ska läggas här:
smb://ugapps01.ug.kth.se/infosys/Virtuellt%20Campus/webtex
